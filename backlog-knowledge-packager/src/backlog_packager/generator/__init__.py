"""Generators — produce output files from classified KnowledgeItems (design §7).

This package depends only on KnowledgeItem, never on the Backlog API,
so all generators are unit-testable without network access.
"""
