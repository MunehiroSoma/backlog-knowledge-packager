"""Normalize raw API responses into KnowledgeItem (design §3.2).

TODO(#7): Markdown conversion, title/encoding normalization, source URL
and timestamp attachment. Items without a source URL must never be
produced (NFR-04).
"""
